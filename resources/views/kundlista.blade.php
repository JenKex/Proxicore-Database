<x-layouts.app>
    <div>
        <p>Kundlista här</p>
        {{-- @foreach ($users as $user)
        <div wire:key="{{ $user->id }}"> 
            <div>
                {{ $user->name }}
            </div>
        </div>
    @endforeach --}}
    </div>
</x-layouts.app>
